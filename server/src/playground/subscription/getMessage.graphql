subscription Notifications {
    subscribeNotifications(input: { userId: 1 }) {
        message
        notification {
            id
            event
            content
            Order {
                orderId
                invoiceNo
            }
            createdAt
            updatedAt
        }
    }
}
