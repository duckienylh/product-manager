export enum DefaultHashValue {
    saltRounds = 10,
}
export enum RoleList {
    director = 999,
    admin = 99,
    manager = 9,
    accountant = 5,
    sales = 1,
    transporterManager = 2,
    driver = 3,
    assistantDriver = 4,
}

export enum BucketValue {
    DEVAPP = 'dev-app',
}
